import React from 'react';
import './Message.css';

const Message = props => (
    
    <div class="ui info compact message">
        <p>Login with your <b>Google</b> account to Submit Adds.</p>
    </div>
 
);

export default Message;